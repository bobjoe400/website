#!/bin/bash
echo "Script executed from: ${PWD}"
for d in /var/www/; do
    cd /var/www/
	jupyter nbconvert --to script *.ipynb
done
for d in /var/www/lib; do
    cd /var/www/lib
    jupyter nbconvert --to script *.ipynb
done
